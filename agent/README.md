# /agent — Milestone 3

The NEET Content Agent: provider-agnostic AI engine (Anthropic default, Gemini
and OpenAI-compatible adapters), strict versioned prompt contracts, validators,
and caching (SPEC §4). Configured entirely via `.env`.
